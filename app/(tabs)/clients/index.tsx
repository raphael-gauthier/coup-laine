import { Surface } from '@/ui/primitives/surface';
import { Text } from '@/ui/primitives/text';

export default function ClientsScreen() {
  return (
    <Surface className="flex-1 items-center justify-center">
      <Text>Clients (J3)</Text>
    </Surface>
  );
}
